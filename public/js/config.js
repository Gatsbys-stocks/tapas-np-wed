// Dirección del backend (API de reservas y opiniones).
//
// - En local (npm start / npm run dev), déjalo vacío: '' — el frontend y la
//   API viven en el mismo servidor, así que las rutas relativas ya funcionan.
// - Publicado en GitHub Pages (o cualquier sitio estático), pon aquí la URL
//   pública de tu backend desplegado en Render/Railway/etc, SIN barra final.
//   Ejemplo: 'https://bar-katmandu-api.onrender.com'
const API_BASE = '';
